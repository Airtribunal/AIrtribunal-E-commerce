import React from 'react';

const Cart = (props) => {
    const { theme } = props 

    return (            
        <section className="cart-section">
            
        </section>
    );
}

export default Cart;
